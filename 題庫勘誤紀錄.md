# 題庫勘誤紀錄

記錄對 `questions_import.csv` / 資料庫題目內容所做的修正，方便日後追蹤「資料庫跟原始題本（`C程式識讀_bangye_v1.pdf`、`python_bangye_v2.pdf`）不一樣的地方」以及為什麼要改。

修正方式：全部直接改資料庫 `Question` 對應欄位（`content` / `option_a`~`option_e`），沒有改 `correct_answer`（除非特別註明）。

---

## 2026-08-25 題庫全面正確性檢查

### 1. Python 題目縮排遺失（全面性問題，已修復）

**問題**：125 題 Python 題目匯入資料庫時，`content` 欄位完全沒有保留程式碼的縮排（每一行都從第 0 格開始）。Python 語法靠縮排決定區塊歸屬，這不只是排版問題——有些題目（例如 Python Q.031）光看沒有縮排的程式碼，會沒辦法判斷 `if` 敘述是巢狀還是並列，兩種讀法答案不一樣。

**驗證方式**：用 `pdftotext -layout` 從原始題本 `python_bangye_v2.pdf` 抽出保留版面位置的文字，確認題本原文本身是有正確縮排的，是匯入流程把縮排弄丟了。

**修復方式**：寫程式比對每題資料庫內容跟題本抽出來的文字（去除空白後逐行比對），把題本裡的縮排空白接回資料庫內容，保留資料庫原本的程式碼書寫風格（如運算子間距）不變。

**結果**：89 題的 `content` 被補上正確縮排；其餘 36 題內容本來就沒有需要縮排的區塊（單行/無巢狀敘述），沒有變動。全部改動都用真實請求驗證過會正確顯示。

### 2. C Q.041（`content_id`=291）：`t//2` 應為 `t/2`

**問題**：`//` 在 C 語言裡是行註解的開頭，不是整數除法。原內容 `t += t//2;` 如果照字面編譯，會被解析成 `t += t;`（`//2;` 整段被當註解吃掉），照這樣執行答案會是 `20`，但標記的正確答案是 `15`（按照「除以 2」的原意推算）。

**驗證方式**：對照題本原文確認題本本身就寫成 `t//2`（不是匯入時打錯），研判是作者同時在寫 Python 題目、手滑帶進來的筆誤。

**修復**：`content` 的 `t += t//2;` 改成 `t += t/2;`。答案 `C`（15）不變，且與修正後的程式碼一致。

### 3. C Q.125（`content_id`=375）：宣告變數 `s`，程式主體卻用 `d`

**問題**：`content` 第一行宣告 `char s[11]="qwaearqtba", ...`，但後面程式碼全程使用從未宣告過的 `d[...]`。照字面編譯是「使用未宣告識別字」的編譯錯誤。

**驗證方式**：對照題本原文確認題本本身就有這個命名不一致（宣告用 `s`、主體用 `d`），且答案解說頁明確是在追蹤 `d` 陣列的變化，確認作者原意是要用同一個名字。

**修復**：把宣告改成 `char d[11]="qwaearqtba", ...`，讓變數名稱與程式主體一致、程式碼合法。答案 `B` 不變。

### 4. C Q.008（`content_id`=258）：`while` 少一個右括號

**問題**：`while (s[i] < 0 && head<tail {` 少了 `)`，純屬題本排版轉譯時漏掉的符號，不影響答案推導，但會讓學生誤以為程式碼有語法錯誤。

**驗證**：對照題本原文確認題本原文就缺這個括號。

**修復**：補上右括號，改成 `while (s[i] < 0 && head<tail) {`。答案 `B` 不變。

### 5. C Q.054（`id`=304）：選項 C、D 都會觸發題目描述的 bug，但只有 C 被標記正確 —— **已改測資**

**問題**：題目程式碼想把陣列 `a`、`b` 兩個遞增序列合併成 `d`，但漏了「迴圈結束後把 `a` 剩下的元素補進 `d` 尾端」，導致 `a` 中大於 `b[3]` 的元素會消失。

- 選項 C（原本標記的正確答案）：`a={3,4,7,9}, b={1,2,5,8}` → 實際執行 `d=[1,2,3,4,5,7,8,-999]`（`-999` 代表從未寫入的記憶體），漏掉 `9`，確實觸發 bug。
- 選項 D（修改前）：`a={1,3,5,7}, b={0,0,0,0}` → 實際執行 `d=[0,0,0,0,-999,-999,-999,-999]`，`a` 的 4 個元素**全部**沒進去——比 C 錯得更明顯，**同樣觸發 bug**，但沒被標記為答案。

用手動追蹤、重讀題本原文（題本自己的解說依據「a 中 >b[3] 的部份沒有在 d 中」套在 D 身上同樣成立）、以及實際用 `gcc` 編譯執行三種方式交叉驗證，確認這是題本作者本身的疏漏，不是匯入或轉譯造成的。

**修復**：把選項 D 的測資從 `a={1,3,5,7}, b={0,0,0,0}` 改成 `a={1,3,5,7}, b={2,4,6,8}`——已用 gcc 編譯驗證這組資料執行結果是完全正確的 `d=[1,2,3,4,5,6,7,8]`，不會觸發任何 bug，能正常當作「非答案」選項。答案 `C` 不變。

**跟原始題本的差異**：此為唯一一處刻意跟題本原文不同的地方（其餘修正都是把資料庫改回跟題本一致），因為題本原文本身的選項設計有瑕疵。

### 6. Python Q.033（`id`=408）：`content` 摻進答案解說文字，直接洩題

**問題**：`content` 是 `'t = 0 (B) 1+3+…+99 = 2500\nfor i in range(100):...'`——`(B) 1+3+…+99 = 2500` 這段答案解說被誤植進題目本文，等於在題目畫面上直接告訴學生答案。全庫掃過一輪，只有這一題有此汙染。

**修復**：把 `content` 裡的 `(B) 1+3+…+99 = 2500` 移除，只留 `t = 0`。答案 `B` 不變（`explanation` 欄位本來就有這段說明，不受影響）。

### 7. Python Q.060（`id`=435）／Python Q.077（`id`=452）：全形連字號誤植成程式碼運算子

**問題**：兩題的程式碼裡各有一個減號被存成 Unicode 全形連字號 `–`（U+2013）而不是正常的 ASCII 減號 `-`。Python 遇到 `–` 這個字元會直接丟出 `SyntaxError: invalid character`，不是能正常執行的減法。

- Q.060：`print(a – b)` → 應為 `print(a - b)`
- Q.077：`if v>t or –v>t:` → 應為 `if v>t or -v>t:`（取絕對值的寫法）

**驗證方式**：全庫掃描所有題目 `content` 欄位裡的智慧排版特殊符號（`– — “ ” …`），只有這兩題命中。研判是題本製作過程中文書軟體「智慧標點」把減號自動轉成連字號。

**修復**：兩題都把 `–` 換回 `-`。答案不變。

### 8. Python Q.106（`id`=481）：多一個語法上不該出現的冒號

**問題**：`content` 裡 `s += sum(a[i:]):` 這一行結尾多了一個冒號——`:` 只能接在 `if`/`for`/`while`/`def` 等控制流程關鍵字後面，接在一般指定敘述後面是語法錯誤，會讓 Python 直接 `SyntaxError`。

**驗證方式**：對照同一份題本裡其他時間複雜度分析題（Q.100、Q.104 等）的程式碼風格，確認這裡不應該有冒號，是單一字元的轉譯瑕疵。

**修復**：拿掉多餘的冒號，改成 `s += sum(a[i:])`。答案 `C` 不變。

### 9. 上一輪縮排修復遺漏 2 題（`id`=457, 467）

**問題**：第 1 項縮排修復批次執行時，這兩題除了程式碼本身，還各多了一行匹配不到的中文說明（`輸出為何？`），修復程式把「有任何一行沒對到」當成整題都不寫入的保守判斷，導致這兩題的縮排其實完全沒被套用（漏判為「需要人工檢查」但後續沒有真的手動處理）。

**修復**：重新比對後確認程式碼部分本來就對得上，只有那行結尾的中文說明本來就不需要縮排、對不到也無妨；重新執行只針對這兩題套用縮排並存檔。已重新用完整測試套件與 `manage.py check` 確認沒有副作用。
